// import React from 'react'
// import ReactDOM from 'react-dom'
import { run } from 'ioperator'
import app from'./app'

const actions = {
  routes: ({ routes }) => {
    //   ReactDOM.render()
  }
};

run(actions, app());